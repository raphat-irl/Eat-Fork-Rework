# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'EAT & FORK Rework' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'Alamofire', '~> 5.0'
  pod 'FSPagerView'
  pod 'Kingfisher'
  
  # Pods for EAT & FORK Rework

  target 'EAT & FORK ReworkTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'EAT & FORK ReworkUITests' do
    # Pods for testing
  end

end
